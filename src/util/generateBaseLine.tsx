import { css } from "styled-components";
// export const generateBaseLine = (fontSize = 16, scale = 2) => {
//   const baseFontSize = 16;
//   const baseLineHeight = 1.5;
//   const capHeight = 0.2; // change this

//   const rhythm = (baseLineHeight * fontSize) / 2;

//   const lines = Math.ceil((fontSize + 0.001) / rhythm);
//   const lineHeight = (rhythm * lines) / fontSize;

//   const baselineDistance = lineHeight - capHeight / 2;

//   return css`
//     font-size: ${fontSize / baseFontSize}rem;
//     // line-height: ${lineHeight};
//     padding-top: ${baselineDistance}em;
//     margin-bottom: ${baseLineHeight - baselineDistance}em;
//   `;
// };
